[project]
name = "global-ai-captions"
version = "0.1.0"
description = "Tally webhook that generates luxury property captions and emails them through Resend."
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.2.0",
    "openai>=3.6.0",
    "requests>=2.34.2",
]
